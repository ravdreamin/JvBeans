# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8080

# GitHub repository URL (for footer link)
NEXT_PUBLIC_GITHUB_URL=https://github.com/ravdreamin

# Optional: Admin token for write operations (POST, PUT, DELETE)
# Leave empty if backend doesn't require authentication
NEXT_PUBLIC_ADMIN_TOKEN=
